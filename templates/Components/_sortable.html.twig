{{ knp_pagination_sortable(articles, 'Date', 'a.createdAt', {'direction': 'DESC', 'class': 'btn btn-secondary'} )}}
{{ knp_pagination_sortable(articles, 'Nom', 'a.createdAt', {'direction': 'DESC', 'class': 'btn btn-secondary'} )}}