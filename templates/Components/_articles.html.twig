{% for article in articles %}
  {% include "Components/_card.html.twig"  with { article : article, admin : (admin is defined) ? admin : false } only %}  
  {% else %}
    <div class="alert  w-100 text-center" role="alert" id="article-no-response">
    <i class="bi bi-x-octagon"></i>
    Il n'y à pas d'article correspondant
    </div>
{% endfor %}